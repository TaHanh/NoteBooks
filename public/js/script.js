// $(document).ready(function(){
  // Initialize Firebase
  var config = {
    apiKey: "AIzaSyCe17iukyL9usrKcBqnuXZDIpayGuLaLYU",
    authDomain: "notebook-f1.firebaseapp.com",
    databaseURL: "https://notebook-f1.firebaseio.com",
    projectId: "notebook-f1",
    storageBucket: "notebook-f1.appspot.com",
    messagingSenderId: "76195646310"
  };
  firebase.initializeApp(config);

  var rootRef = firebase.database().ref();
  console.log(rootRef);

  var provider = new firebase.auth.GoogleAuthProvider();
// firebase.auth().onAuthStateChanged(function(user) {
//   var user = firebase.auth().currentUser;
//   console.log(user);
//   if (user) {
//     alert("Đăng nhập thành công")
//   } else {
//     // No user is signed in.
//   }
// });
 function login() {
   // var user = document.getElementById("userTxt").value;
   // var pass = document.getElementById("passTxt").value;
   
//    firebase.auth().createUserWithEmailAndPassword(user, pass).catch(function(error) {
//   // Handle Errors here.
//   var errorCode = error.code;
//   var errorMessage = error.message;
//   alert(errorMessage);
//   // ...
// });

   // xác thực mail đã tồn tại

firebase.auth().getRedirectResult().then(function(result) {

































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































  
  if (result.credential) {
    // Lấy Google Access Token để truy cập Google API.
    var token = result.credential.accessToken;
    // ...
  }
  // Tạo biến user để chứa thông tin user.
  var user = result.user;
})..catch(function(error) {
  // Xử lí lỗi nếu có phát sinh
  var errorCode = error.code;
  var errorMessage = error.message;
  // Email của người dùng
  var email = error.email;
  // Xác định firebase.auth.AuthCredential type
  var credential = error.credential;
  // ...
});

 }
// })
